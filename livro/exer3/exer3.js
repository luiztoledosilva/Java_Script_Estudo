/**
 *   A Lei de Ohm define que a resistência (R) de um condutor 
 * é obtida por meio da divisão da tensão aplicada (V) 
 * dividida pela intensidade de corrente elétrica (A). Dessa forma, 
 * a partir de uma tensão e corrente digitadas pelo usuário em um formulário HTML, 
 * utilize o JavaScript para calcular e mostrar o valor da resistência 
 * na mesma página web que contém o formulário.

 
 */

function calcular() {

    res.value = (parseFloat(volts.value)) / (parseFloat(corrente.value))


}